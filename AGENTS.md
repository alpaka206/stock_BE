# AGENTS.md

## 목적

이 저장소는 주식 리서치 서비스의 메인 백엔드다. 프런트는 외부 금융 API를 직접 호출하지 않고, 백엔드가 수집·정규화·저장한 자료를 읽는다.

## 기술 선택

- 메인 API: Spring Boot 3, Java 17
- 저장소: Postgres
- 마이그레이션: Flyway
- 캐시/큐 확장: Redis
- 프런트: Next.js는 화면과 얇은 BFF 역할
- 데이터/AI/더빙 worker: 필요하면 Python 또는 별도 서비스로 분리

## 데이터 원칙

- 가격, 뉴스, 공시, 이벤트, 영상·오디오 자료는 백엔드에 먼저 저장한다.
- 프런트에는 저장된 자료와 출처를 함께 내려준다.
- 출처 없는 수치와 설명은 만들지 않는다.
- 목데이터를 운영 응답처럼 위장하지 않는다.
- 사용자 판단 기록, 리포트 예약, 구독 상태, 더빙 작업 상태는 서버 DB에 저장한다.

## Git 원칙

- 기준 브랜치: `develop`
- 릴리스 브랜치: `main`
- 초기 저장소 부트스트랩 이후에는 `main`, `develop` 직접 push를 피한다.
- squash merge는 사용하지 않는다.
- 커밋과 PR 제목에는 `[codex]`를 붙이지 않는다.
- 커밋 메시지는 `feat:`, `fix:`, `docs:`, `chore:`, `ci:`, `test:`, `perf:` 중 하나로 시작한다.

## 검증

```powershell
.\mvnw.cmd test
```

## 보안

- `.env`, `.env.*`, API 키, 토큰, 웹훅 URL은 커밋하지 않는다.
- `.env.example`에는 placeholder만 둔다.
- 구독별 기능 제한 UI는 만들 수 있지만, 접근 제한은 사용자가 요청하기 전까지 강제하지 않는다.
